#!/usr/bin/python3
def multiply_by_2(a_dictionary):
        multi_dir = a_dictionary.copy()
    list_keys = list(multi_dir.keys())

    for i in list_keys:
        for_dir[i] *= 2

    return (multi_dir)
